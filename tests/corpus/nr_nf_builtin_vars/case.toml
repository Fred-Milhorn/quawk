id = "nr_nf_builtin_vars"
description = "NR, FNR, and NF update as records are read."
program = "program.awk"
input = "input.txt"
tags = ["builtin-vars", "runtime", "p8"]

[expect]
stdout = "expected.stdout"
exit = 0
