id = "begin_logical_and"
description = "Grouped logical AND expression in BEGIN."
program = "program.awk"
tags = ["supported", "compat-baseline", "p4", "expressions"]

[expect]
stdout = "expected.stdout"
exit = 0
