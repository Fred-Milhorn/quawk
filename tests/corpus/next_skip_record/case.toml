id = "next_skip_record"
description = "The next statement skips the remainder of the current record and continues with the next one."
program = "program.awk"
input = "input.txt"
tags = ["supported", "runtime", "compat-baseline", "next", "p8"]

[expect]
stdout = "expected.stdout"
exit = 0
