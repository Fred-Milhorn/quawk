id = "function_basic_call"
description = "A basic user-defined function call executes and returns its result."
program = "program.awk"
tags = ["supported", "functions", "p5"]

[expect]
stdout = "expected.stdout"
exit = 0
