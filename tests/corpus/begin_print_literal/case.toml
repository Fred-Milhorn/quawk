id = "begin_print_literal"
description = "Literal string print from BEGIN."
program = "program.awk"
tags = ["supported", "compat-baseline", "smoke", "p1"]

[expect]
stdout = "expected.stdout"
exit = 0
