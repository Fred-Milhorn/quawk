id = "unset_scalar_coercion"
description = "An unset scalar reads as 0 in numeric context and the empty string in string context."
program = "program.awk"
tags = ["supported", "runtime", "expressions", "p8"]

[expect]
stdout = "expected.stdout"
exit = 0
