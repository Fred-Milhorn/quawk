id = "for_expr_list_loop"
description = "Classic for loops accept comma-operator expressions in init and update positions."
program = "program.awk"
tags = ["supported", "loops", "p10", "posix"]

[expect]
stdout = "expected.stdout"
exit = 0
