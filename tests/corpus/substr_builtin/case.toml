id = "substr_builtin"
description = "The substr builtin returns the requested slice."
program = "program.awk"
tags = ["supported", "builtin", "substr", "compat-baseline", "p8"]

[expect]
stdout = "expected.stdout"
exit = 0
