id = "begin_if_less"
description = "Simple comparison and if statement in BEGIN."
program = "program.awk"
tags = ["supported", "compat-baseline", "p2", "control-flow"]

[expect]
stdout = "expected.stdout"
exit = 0
