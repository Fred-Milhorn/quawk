id = "regex_filter"
description = "Regex-driven record filtering."
program = "program.awk"
input = "input.txt"
tags = ["supported", "regex", "posix-required"]

[expect]
stdout = "expected.stdout"
exit = 0
