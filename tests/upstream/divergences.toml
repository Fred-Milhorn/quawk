# Evaluated divergences for the runnable upstream compatibility slice.
#
# Keep this manifest empty until an executed upstream-selected case has been
# reviewed and intentionally classified. Each entry must also reference a note
# marker in docs/compatibility-evaluations.md.
